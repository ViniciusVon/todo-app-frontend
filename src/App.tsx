import { RoutesConst } from "routes";
import { Theme, GlobalStyles } from "themes";

export const App = () => {
  return (
    <Theme>
        <GlobalStyles />
        <RoutesConst />
    </Theme>
  )
}

export default App;
